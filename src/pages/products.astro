---
import ProductCatalog from '../components/ProductCatalog.astro';

// /products defaults to the first line (data); each line's tab page lives at
// /products/<line> (see products/[line].astro).
---

<ProductCatalog locale="en" line="data" />
