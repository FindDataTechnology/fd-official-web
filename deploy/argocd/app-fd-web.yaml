# ArgoCD Application for fd-official-web.
# Applied once manually (bootstrap); ArgoCD then watches deploy/k8s/ in this repo.
# This file lives OUTSIDE deploy/k8s so the Application is not self-managed.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fd-web
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/FindDataTechnology/fd-official-web.git
    targetRevision: main
    path: deploy/k8s
    # deploy/k8s/ 还有 librechat.yaml / mcp.yaml（人工管理，非本 app 范围）——
    # 必须过滤，否则 ArgoCD 会收养甚至 prune 其他工作负载
    directory:
      include: fd-web.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: fd-web
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
